# easy-agent-mem v0.4.2

## Highlights

- Added a real `watch` command for automated checkpoint summaries
- Rewrote the README into a proper GitHub landing page
- Added reusable release-note files under `docs/releases/`

## What Changed

### Automatic checkpointing

- Added `agent-mem watch`
- Watches the repo for file activity
- Waits for a quiet period
- Writes an automatic checkpoint summary after enough files change

### README

- Reworked the GitHub README to explain:
  - the product purpose
  - Obsidian mode
  - fallback mode
  - summarize / recall / watch
  - IDE integration
  - release-note history

### Release-note assets

- Added release-note documents to reuse as GitHub release bodies

## Verification

- source compile check passed
- instruction generation checks passed
- packaging build passed
- `twine check` passed
- wheel contents were verified

## Upgrade

```bash
pip install -U easy-agent-mem
```
